import { PageContainer } from "../../styles/styles";
import styled from "@emotion/styled";

export const ProductPageContainer = styled(PageContainer)`
  display: flex;
  flex-direction: column;
  align-items: center;
`